/**
 * The toolbar has all the different menus which let user select things like colors(for
 * canvas,background pen), Language and Turte Design.
 */
public interface ToolBar {

  /**
   * @return returns the JavaFX MenuBar object so it can be added into the main root.
   */
  public MenuBar buildToolBar() {

  }
}